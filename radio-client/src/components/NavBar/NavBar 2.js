import React, { Component } from "react";

export default class NavBar extends Component {
  render() {
    return( 
      <div style={styles.main}>
        navBar
      </div>
    )}
}

const styles = {
  main: {
    height: '14vh'
  }
}