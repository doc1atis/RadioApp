import React from 'react';
import playButton from './playButton.jpg';

playButton = () => {
    return(
        <div>
            <img src={playButton} alt="Play button to start playing radio station stream " />
        </div>
    )
}

export default playButton