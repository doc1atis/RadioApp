import React from 'react';
import stopButton from './stopButton.jpg';

stopButton = () => {
    return(
        <div>
            <img src={stopButton} alt="Stop button that stops radio stream" />
        </div>
    )
}

export default stopButton